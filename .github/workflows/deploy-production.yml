name: Deploy to Production

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag (e.g. v1.2.3) to deploy'
        required: true
        default: 'v1.0.0'

env:
  NODE_VERSION: '20'
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-test:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag || github.ref }}

      - name: Use Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: Type check
        run: npx tsc --noEmit

      - name: Build application
        run: npm run build
        env:
          DATABASE_URL: postgresql://dummy:dummy@localhost:5432/dummy

  build-image:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    needs: build-test
    permissions:
      contents: read
      packages: write

    outputs:
      version: ${{ steps.version.outputs.version }}
      tags: ${{ steps.meta.outputs.tags }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag || github.ref }}

      - name: Determine version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION=${{ github.event.inputs.tag }}
          else
            VERSION=${GITHUB_REF_NAME}
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=tag
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=latest,enable=true

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            NODE_ENV=production

  deploy:
    name: Deploy to Contabo
    runs-on: ubuntu-latest
    needs: build-image
    environment:
      name: production
      url: https://cycleparadise.bike

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        env:
          VERSION: ${{ needs.build-image.outputs.version }}
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
          SESSION_SECRET: ${{ secrets.SESSION_SECRET }}
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          CONTACT_EMAIL: ${{ secrets.CONTACT_EMAIL }}
          PUBLIC_SITE_URL: ${{ secrets.PUBLIC_SITE_URL }}
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          envs: VERSION,POSTGRES_PASSWORD,SESSION_SECRET,SMTP_HOST,SMTP_PORT,SMTP_USER,SMTP_PASS,CONTACT_EMAIL,PUBLIC_SITE_URL
          script: |
            set -euo pipefail
            
            echo "Deploying version: $VERSION"

            # Fix git ownership issue for deploy user
            git config --global --add safe.directory /opt/cycleparadise

            cd /opt/cycleparadise
            
            # Completely stop and remove Docker Compose project to clear any cached state
            docker compose down --remove-orphans --volumes 2>/dev/null || true
            docker compose rm -f -s -v 2>/dev/null || true
            
            # Remove Docker Compose cache/config that might store old variable references
            rm -rf .docker 2>/dev/null || true
            
            # Remove ALL potential sources of stale environment variables
            rm -f .env .env.* docker-compose.override.yml docker-compose.*.yml
            
            # Clean any local changes and fetch
            git reset --hard HEAD
            git clean -ffdx
            git fetch --all --tags --force
            
            # Force checkout the version
            git checkout --force "$VERSION"
            
            # Create fresh .env file
            rm -f .env
            
            # Escape dollar signs in password for Docker Compose variable interpolation
            ESCAPED_PASSWORD=$(echo "${POSTGRES_PASSWORD}" | sed 's/\$/\$\$/g')
            
            # URL-encode the password for PostgreSQL connection string
            URL_ENCODED_PASSWORD=$(python3 -c "import urllib.parse; print(urllib.parse.quote('${POSTGRES_PASSWORD}', safe=''))")
            
            cat > .env <<EOF
            DB_USER=cycleparadise
            DB_PASSWORD=${ESCAPED_PASSWORD}
            DB_NAME=cycleparadise
            DATABASE_URL="postgresql://cycleparadise:${URL_ENCODED_PASSWORD}@db:5432/cycleparadise?schema=public"
            DIRECT_URL="postgresql://cycleparadise:${URL_ENCODED_PASSWORD}@db:5432/cycleparadise?schema=public"
            SESSION_SECRET=${SESSION_SECRET}
            NODE_ENV=production
            SMTP_HOST=${SMTP_HOST}
            SMTP_PORT=${SMTP_PORT}
            SMTP_USER=${SMTP_USER}
            SMTP_PASSWORD=${SMTP_PASS}
            FROM_EMAIL=noreply@cycleparadise.bike
            ADMIN_EMAIL=admin@cycleparadise.bike
            CONTACT_EMAIL=${CONTACT_EMAIL}
            PUBLIC_SITE_URL=${PUBLIC_SITE_URL}
            IMAGE_QUALITY=80
            MAX_IMAGE_SIZE=2048
            EOF
            
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            
            # Use explicit project directory and env file for all docker compose commands
            export COMPOSE_PROJECT_NAME=cycleparadise
            export COMPOSE_FILE=/opt/cycleparadise/docker-compose.yml
            
            # Now use the updated docker-compose.yml for all operations
            TAG=$VERSION docker compose --env-file .env pull
            
            # Force rebuild images with the new docker-compose.yml
            TAG=$VERSION docker compose --env-file .env build --no-cache
            
            # Start containers with explicit --env-file to force our .env
            set +e
            TAG=$VERSION docker compose --env-file .env up -d
            COMPOSE_EXIT=$?
            set -e
            
            # Wait for containers to be ready
            sleep 10
            
            # Check if web container is running
            if ! docker compose --env-file .env ps web | grep -q "Up"; then
              echo "ERROR: Web container is not running"
              docker compose --env-file .env logs web
              exit 1
            fi
            
            echo "Deployment completed successfully"
            docker compose --env-file .env ps
            
            docker image prune -af --filter "until=72h"

  health-check:
    name: Health Check
    runs-on: ubuntu-latest
    needs: deploy

    steps:
      - name: Wait for rollout
        run: sleep 15

      - name: Check homepage
        run: |
          STATUS=$(curl -sL -o /dev/null -w "%{http_code}" https://cycleparadise.bike)
          if [ "$STATUS" != "200" ]; then
            echo "Unexpected status: $STATUS" && exit 1
          fi

      - name: Check health endpoint
        run: |
          curl -sfL https://cycleparadise.bike/health > /dev/null
